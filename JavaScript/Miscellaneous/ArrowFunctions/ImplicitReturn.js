// General manner
function randomNumber(){
  return Math.random()
}

// Simplicity if possible
const randomNumber = () => Math.random() // as randomNumber has no params, a void set of parenthesis is used

// As seen on https://blog.webdevsimplified.com/2020-09/arrow-functions/

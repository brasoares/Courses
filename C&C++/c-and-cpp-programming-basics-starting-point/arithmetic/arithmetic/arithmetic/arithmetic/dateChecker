#include <stdio.h>
#include <math.h>
// variable
int days = 0;

int main () {
	
	printf("Enter the total amount of days: \n");
	scanf("%d", &days);
	
	printf("You have lived, so far: %d years, %d months and %d days!", (days/365), ((days%365)/30), ((days%365)%30));
	return 0;
}
